# Стоимость (`cost`)

Стоимость определяет, *чем игрок платит* за каст.

Формат:

```yml
cost:
  xpLevels: 1
```

или

```yml
cost:
  material: "EMERALD"
  amount: 2
```

Если ресурсов не хватает — каст отклоняется.

## Варианты

### XP уровни

- `xpLevels: number` — списать указанное количество уровней

### Предмет

- `material: string` — материал Bukkit
- `amount: number` — количество (по умолчанию 1)

## См. также

- [Условия](04-Условия.md)
- [config.yml (messages)](../03-config.md)
